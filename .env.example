# cp .env.example .env  (.env is gitignored)
# A $ in a value is a compose variable reference; write it as $$.

GRAFANA_ADMIN_USER=admin

# required, no default. Generate: openssl rand -hex 32
GRAFANA_ADMIN_PASSWORD=

# sample retention; drives the size of the prometheus_data volume
PROM_RETENTION=15d
